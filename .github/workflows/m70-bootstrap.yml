name: M70 Bootstrap

on:
  push:
    branches: [feat/m70-crisis-unified-player-president-runtime]
    paths:
      - '.github/workflows/m70-bootstrap.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: feat/m70-crisis-unified-player-president-runtime

      - name: Set up Dart
        uses: dart-lang/setup-dart@v1
        with:
          sdk: stable

      - name: Install dependencies
        run: dart pub get

      - name: Apply M70 implementation
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f'{path}: expected one replacement, found {count}')
              p.write_text(text.replace(old, new, 1))

          def write(path, content):
              p = Path(path)
              p.parent.mkdir(parents=True, exist_ok=True)
              p.write_text(content)

          # M65: expose the existing M47 crisis integration seam without changing defaults.
          replace_once(
              'lib/src/facility/player_president_tenure_gated_ticket_pricing_runtime_integration.dart',
              "import '../crisis/facility_sponsor_crisis_runtime_composition.dart';\n",
              "import '../crisis/crisis_runtime_integration.dart';\nimport '../crisis/facility_sponsor_crisis_runtime_composition.dart';\n",
          )
          replace_once(
              'lib/src/facility/player_president_tenure_gated_ticket_pricing_runtime_integration.dart',
              "    this.sponsorSystem = const SponsorSystemEngine(),\n    this.sourceEngine = const PromiseMediaCareerEngine(),\n",
              "    this.sponsorSystem = const SponsorSystemEngine(),\n    this.crisisIntegration = const CrisisRuntimeIntegrationEngine(),\n    this.sourceEngine = const PromiseMediaCareerEngine(),\n",
          )
          replace_once(
              'lib/src/facility/player_president_tenure_gated_ticket_pricing_runtime_integration.dart',
              "  final SponsorSystemEngine sponsorSystem;\n  final PromiseMediaCareerEngine sourceEngine;\n",
              "  final SponsorSystemEngine sponsorSystem;\n  final CrisisRuntimeIntegrationEngine crisisIntegration;\n  final PromiseMediaCareerEngine sourceEngine;\n",
          )
          replace_once(
              'lib/src/facility/player_president_tenure_gated_ticket_pricing_runtime_integration.dart',
              "        sponsorSystem: sponsorSystem,\n        baseWorldEngine: pricedWorld,\n        sourceEngine: sourceEngine,\n",
              "        sponsorSystem: sponsorSystem,\n        baseWorldEngine: pricedWorld,\n        crisisIntegration: crisisIntegration,\n        sourceEngine: sourceEngine,\n",
          )

          # M68: thread the neutral crisis seam into M65.
          replace_once(
              'lib/src/facility/player_president_tenure_gated_facility_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "import '../crisis/facility_sponsor_crisis_runtime_composition.dart';\n",
              "import '../crisis/crisis_runtime_integration.dart';\nimport '../crisis/facility_sponsor_crisis_runtime_composition.dart';\n",
          )
          replace_once(
              'lib/src/facility/player_president_tenure_gated_facility_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "    this.investment = const PresidentFacilityInvestmentRuntimeEngine(),\n    this.sponsorSystem = const SponsorSystemEngine(),\n",
              "    this.investment = const PresidentFacilityInvestmentRuntimeEngine(),\n    this.sponsorSystem = const SponsorSystemEngine(),\n    this.crisisIntegration = const CrisisRuntimeIntegrationEngine(),\n",
          )
          replace_once(
              'lib/src/facility/player_president_tenure_gated_facility_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "  final PresidentFacilityInvestmentRuntimeEngine investment;\n  final SponsorSystemEngine sponsorSystem;\n",
              "  final PresidentFacilityInvestmentRuntimeEngine investment;\n  final SponsorSystemEngine sponsorSystem;\n  final CrisisRuntimeIntegrationEngine crisisIntegration;\n",
          )
          replace_once(
              'lib/src/facility/player_president_tenure_gated_facility_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "      sponsorSystem: sponsorSystem,\n      sourceEngine: sourceEngine,\n",
              "      sponsorSystem: sponsorSystem,\n      crisisIntegration: crisisIntegration,\n      sourceEngine: sourceEngine,\n",
          )

          # M69: keep sponsor composition intact while accepting the same neutral crisis seam.
          replace_once(
              'lib/src/sponsor/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "import '../crisis/player_president_facility_control.dart';\n",
              "import '../crisis/crisis_runtime_integration.dart';\nimport '../crisis/player_president_facility_control.dart';\n",
          )
          replace_once(
              'lib/src/sponsor/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "    this.investment = const PresidentFacilityInvestmentRuntimeEngine(),\n  });\n",
              "    this.investment = const PresidentFacilityInvestmentRuntimeEngine(),\n    this.crisisIntegration = const CrisisRuntimeIntegrationEngine(),\n  });\n",
          )
          replace_once(
              'lib/src/sponsor/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "  final PresidentFacilityInvestmentRuntimeEngine investment;\n",
              "  final PresidentFacilityInvestmentRuntimeEngine investment;\n  final CrisisRuntimeIntegrationEngine crisisIntegration;\n",
          )
          replace_once(
              'lib/src/sponsor/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "      sponsorSystem: sponsorSystem,\n    );\n",
              "      sponsorSystem: sponsorSystem,\n      crisisIntegration: crisisIntegration,\n    );\n",
          )

          write(
              'lib/src/crisis/player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              r'''import '../core/simulation_config.dart';
import '../election/player_president_tenure_control_gate.dart';
import '../election/president_tenure.dart';
import '../facility/player_president_tenure_gated_ticket_pricing_control.dart';
import '../facility/player_president_tenure_gated_ticket_pricing_runtime_integration.dart';
import '../facility/stadium_facility.dart';
import '../league/club.dart';
import '../media/player_president_media_statement_control.dart';
import '../promise/player_president_promise_control.dart';
import '../sponsor/player_president_sponsor_control.dart';
import '../sponsor/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart';
import '../sponsor/sponsor_system.dart';
import '../transfer/player_president_transfer_strategy_control.dart';
import '../world/world_career_engine.dart';
import '../world/world_league.dart';
import 'crisis_decision_core.dart';
import 'crisis_runtime_integration.dart';
import 'player_president_crisis_control.dart';
import 'player_president_facility_control.dart';
import 'president_facility_investment_runtime_integration.dart';

/// M70 adds M51 crisis response control to M69's unified player-president
/// runtime without creating another checkpoint or save codec.
///
/// M65's [PlayerPresidentTicketPricingRuntimeCheckpoint] remains authoritative.
/// The player crisis provider is consulted only for a real crisis belonging to
/// the controlled club while the captured player-president still owns the real
/// incumbent identity. Persisted loss starts blocked, an incumbent mismatch is
/// sticky for the in-flight runtime, and all other clubs stay on the canonical
/// M43 AI crisis path.
class PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine {
  const PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine({
    this.crisisProvider,
    this.sponsorProvider,
    this.facilityProvider,
    this.promiseProvider,
    this.mediaProvider,
    this.transferStrategyProvider,
    this.ticketPricingProvider,
    this.offerEngine = const SponsorOfferEngine(),
    this.aiSponsorPolicy = const PresidentSponsorDecisionPolicy(),
    this.aiCrisisEngine = const CrisisDecisionEngine(activationThreshold: 55),
    this.ticketAiPolicy = const PresidentMatchdayTicketPricingPolicy(),
    this.pricingPolicy = const MatchdayTicketPricingPolicy(),
    this.stadiumPolicy = const StadiumInvestmentPolicy(),
    this.tenureGate = const PlayerPresidentTenureControlGate(),
    this.baseWorldEngine = const WorldCareerEngine(),
    this.investment = const PresidentFacilityInvestmentRuntimeEngine(),
  });

  final PlayerCrisisDecisionProvider? crisisProvider;
  final PlayerSponsorDecisionProvider? sponsorProvider;
  final PlayerFacilityInvestmentDecisionProvider? facilityProvider;
  final PlayerPromiseDecisionProvider? promiseProvider;
  final PlayerMediaStatementDecisionProvider? mediaProvider;
  final PlayerTransferStrategyDecisionProvider? transferStrategyProvider;
  final PlayerMatchdayTicketPricingDecisionProvider? ticketPricingProvider;
  final SponsorOfferEngine offerEngine;
  final PresidentSponsorDecisionPolicy aiSponsorPolicy;
  final CrisisDecisionEngine aiCrisisEngine;
  final PresidentMatchdayTicketPricingPolicy ticketAiPolicy;
  final MatchdayTicketPricingPolicy pricingPolicy;
  final StadiumInvestmentPolicy stadiumPolicy;
  final PlayerPresidentTenureControlGate tenureGate;
  final WorldCareerEngine baseWorldEngine;
  final PresidentFacilityInvestmentRuntimeEngine investment;

  PlayerPresidentTicketPricingRuntimeCareerResult simulateWithCheckpoint({
    required List<Club> clubs,
    required List<WorldLeague> leagues,
    required SimulationConfig config,
    required String controlledClubId,
    int seasonCount = 20,
    int electionInterval = 4,
    bool hasFutureSeasonAfterReport = false,
  }) {
    if (!clubs.any((club) => club.id == controlledClubId)) {
      throw ArgumentError('Unknown controlled club $controlledClubId.');
    }
    final initialPresident = const PresidentProfileGenerator().generateInitial(
      clubId: controlledClubId,
      careerSeed: config.careerSeed,
      simulationVersion: config.simulationVersion,
    );
    final session = _M70CrisisTenureSession(
      playerPresidentId: initialPresident.id,
      blocked: false,
    );
    return _delegate(
      controlledClubId: controlledClubId,
      session: session,
    ).simulateWithCheckpoint(
      clubs: clubs,
      leagues: leagues,
      config: config,
      controlledClubId: controlledClubId,
      seasonCount: seasonCount,
      electionInterval: electionInterval,
      hasFutureSeasonAfterReport: hasFutureSeasonAfterReport,
    );
  }

  PlayerPresidentTicketPricingRuntimeCareerResult resume({
    required PlayerPresidentTicketPricingRuntimeCheckpoint checkpoint,
    required int seasonCount,
    bool hasFutureSeasonAfterReport = false,
  }) {
    checkpoint.validate();
    final tenure = checkpoint.tenureControl;
    final session = _M70CrisisTenureSession(
      playerPresidentId: tenure.playerPresidentId,
      blocked: tenure.lost,
    );
    return _delegate(
      controlledClubId: tenure.controlledClubId,
      session: session,
    ).resume(
      checkpoint: checkpoint,
      seasonCount: seasonCount,
      hasFutureSeasonAfterReport: hasFutureSeasonAfterReport,
    );
  }

  PlayerPresidentTenureGatedFacilitySponsorPromiseMediaTransferTicketPricingRuntimeCareerEngine
      _delegate({
    required String controlledClubId,
    required _M70CrisisTenureSession session,
  }) {
    final CrisisRuntimeIntegrationEngine crisisIntegration;
    if (crisisProvider == null || session.blocked) {
      crisisIntegration = CrisisRuntimeIntegrationEngine(
        decisionEngine: aiCrisisEngine,
      );
    } else {
      crisisIntegration = CrisisRuntimeIntegrationEngine(
        decisionEngine: _M70PlayerPresidentCrisisDecisionEngine(
          controlledClubId: controlledClubId,
          provider: _M70TenureGatedCrisisProvider(
            delegate: crisisProvider!,
            session: session,
          ),
          aiEngine: aiCrisisEngine,
        ),
      );
    }
    return PlayerPresidentTenureGatedFacilitySponsorPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      sponsorProvider: sponsorProvider,
      facilityProvider: facilityProvider,
      promiseProvider: promiseProvider,
      mediaProvider: mediaProvider,
      transferStrategyProvider: transferStrategyProvider,
      ticketPricingProvider: ticketPricingProvider,
      offerEngine: offerEngine,
      aiSponsorPolicy: aiSponsorPolicy,
      ticketAiPolicy: ticketAiPolicy,
      pricingPolicy: pricingPolicy,
      stadiumPolicy: stadiumPolicy,
      tenureGate: tenureGate,
      baseWorldEngine: baseWorldEngine,
      investment: investment,
      crisisIntegration: crisisIntegration,
    );
  }
}

class _M70CrisisTenureSession {
  _M70CrisisTenureSession({
    required this.playerPresidentId,
    required bool blocked,
  }) : _blocked = blocked;

  final String playerPresidentId;
  bool _blocked;

  bool get blocked => _blocked;

  bool allows(String presidentId) {
    if (_blocked) return false;
    if (presidentId == playerPresidentId) return true;
    _blocked = true;
    return false;
  }
}

class _M70TenureGatedCrisisProvider extends PlayerCrisisDecisionProvider {
  const _M70TenureGatedCrisisProvider({
    required this.delegate,
    required this.session,
  });

  final PlayerCrisisDecisionProvider delegate;
  final _M70CrisisTenureSession session;

  @override
  PlayerCrisisActionChoice choose(PlayerCrisisDecisionContext context) {
    if (session.allows(context.presidentId)) {
      return delegate.choose(context);
    }
    return PlayerCrisisActionChoice(action: context.aiDecision.action);
  }
}

class _M70PlayerPresidentCrisisDecisionEngine extends CrisisDecisionEngine {
  _M70PlayerPresidentCrisisDecisionEngine({
    required this.controlledClubId,
    required this.provider,
    required this.aiEngine,
  }) : super(activationThreshold: aiEngine.activationThreshold);

  final String controlledClubId;
  final PlayerCrisisDecisionProvider provider;
  final CrisisDecisionEngine aiEngine;

  @override
  CrisisResolution? evaluate(CrisisContext context) {
    if (context.clubId != controlledClubId) {
      return aiEngine.evaluate(context);
    }

    final scenario = aiEngine.detect(context);
    if (scenario == null) {
      return null;
    }
    final aiDecision = aiEngine.choose(
      scenario: scenario,
      president: context.president,
    );
    final available = aiEngine.availableDecisions(scenario);
    final playerContext = PlayerCrisisDecisionContext(
      crisis: context,
      scenario: scenario,
      availableDecisions: available,
      aiDecision: aiDecision,
    );
    final choice = provider.choose(playerContext);
    final matches = available
        .where((decision) => decision.action == choice.action)
        .toList(growable: false);
    if (matches.length != 1) {
      throw ArgumentError.value(
        choice.action,
        'action',
        'Player crisis choice must select one action for ${scenario.type.name}.',
      );
    }
    return aiEngine.resolveAction(
      context: context,
      scenario: scenario,
      action: matches.single.action,
    );
  }
}
''',
          )

          write(
              'lib/player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              "export 'src/crisis/player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart';\n",
          )

          write(
              'test/m70_player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition_test.dart',
              r'''import 'package:futbol_baskanlik_m0/crisis_runtime_integration.dart';
import 'package:futbol_baskanlik_m0/futbol_baskanlik_m0.dart';
import 'package:futbol_baskanlik_m0/player_president_crisis_control.dart';
import 'package:futbol_baskanlik_m0/player_president_facility_control.dart';
import 'package:futbol_baskanlik_m0/player_president_media_statement_control.dart';
import 'package:futbol_baskanlik_m0/player_president_promise_control.dart';
import 'package:futbol_baskanlik_m0/player_president_sponsor_control.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_promise_media_transfer_ticket_pricing_runtime_composition.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_ticket_pricing_runtime_integration.dart';
import 'package:futbol_baskanlik_m0/player_president_transfer_strategy_control.dart';
import 'package:test/test.dart';

class _AlwaysBalancedPolicy extends PresidentMatchdayTicketPricingPolicy {
  const _AlwaysBalancedPolicy();

  @override
  MatchdayTicketPricingChoice choose({
    required PresidentManagementProfile profile,
    required int fanTrust,
    required StadiumAttendanceProfile base,
  }) =>
      const MatchdayTicketPricingChoice(MatchdayTicketPriceTier.balanced);
}

class _PremiumTicketProvider
    extends PlayerMatchdayTicketPricingDecisionProvider {
  const _PremiumTicketProvider();

  @override
  MatchdayTicketPricingChoice choose(
    PlayerPresidentTicketPricingDecisionContext context,
  ) =>
      const MatchdayTicketPricingChoice(MatchdayTicketPriceTier.premium);
}

class _CountingPremiumTicketProvider
    extends PlayerMatchdayTicketPricingDecisionProvider {
  int calls = 0;

  @override
  MatchdayTicketPricingChoice choose(
    PlayerPresidentTicketPricingDecisionContext context,
  ) {
    calls++;
    return const MatchdayTicketPricingChoice(MatchdayTicketPriceTier.premium);
  }
}

class _YouthTransferProvider extends PlayerTransferStrategyDecisionProvider {
  const _YouthTransferProvider();

  @override
  PlayerTransferStrategyChoice chooseTransferStrategy(
    PlayerTransferStrategyDecisionContext context,
  ) =>
      const PlayerTransferStrategyChoice(
        financialDiscipline: 60,
        transferAmbition: 60,
        riskAppetite: 60,
        youthOrientation: 90,
      );
}

class _CountingYouthTransferProvider
    extends PlayerTransferStrategyDecisionProvider {
  int calls = 0;

  @override
  PlayerTransferStrategyChoice chooseTransferStrategy(
    PlayerTransferStrategyDecisionContext context,
  ) {
    calls++;
    return const PlayerTransferStrategyChoice(
      financialDiscipline: 60,
      transferAmbition: 60,
      riskAppetite: 60,
      youthOrientation: 90,
    );
  }
}

class _AlternativePromiseProvider extends PlayerPromiseDecisionProvider {
  const _AlternativePromiseProvider();

  @override
  PresidentPromiseType choosePromise(PlayerPromiseDecisionContext context) =>
      context.allowedTypes.firstWhere(
        (type) => type != context.aiPromise.type,
        orElse: () => context.aiPromise.type,
      );
}

class _CountingAlternativePromiseProvider
    extends PlayerPromiseDecisionProvider {
  int calls = 0;

  @override
  PresidentPromiseType choosePromise(PlayerPromiseDecisionContext context) {
    calls++;
    return context.allowedTypes.firstWhere(
      (type) => type != context.aiPromise.type,
      orElse: () => context.aiPromise.type,
    );
  }
}

class _AlternativeMediaProvider extends PlayerMediaStatementDecisionProvider {
  const _AlternativeMediaProvider();

  @override
  MediaStance chooseStance(PlayerMediaStatementDecisionContext context) =>
      context.allowedStances.firstWhere(
        (stance) => stance != context.aiStatement.stance,
      );
}

class _CountingAlternativeMediaProvider
    extends PlayerMediaStatementDecisionProvider {
  int calls = 0;

  @override
  MediaStance chooseStance(PlayerMediaStatementDecisionContext context) {
    calls++;
    return context.allowedStances.firstWhere(
      (stance) => stance != context.aiStatement.stance,
    );
  }
}

class _HoldFacilityProvider extends PlayerFacilityInvestmentDecisionProvider {
  const _HoldFacilityProvider();

  @override
  PlayerFacilityInvestmentChoice choose(
    PlayerFacilityInvestmentContext context,
  ) =>
      PlayerFacilityInvestmentChoice.hold;
}

class _CountingHoldFacilityProvider
    extends PlayerFacilityInvestmentDecisionProvider {
  int calls = 0;

  @override
  PlayerFacilityInvestmentChoice choose(PlayerFacilityInvestmentContext context) {
    calls++;
    return PlayerFacilityInvestmentChoice.hold;
  }
}

class _NonAiSponsorProvider extends PlayerSponsorDecisionProvider {
  const _NonAiSponsorProvider();

  @override
  PlayerSponsorOfferChoice choose(PlayerSponsorDecisionContext context) {
    final alternative = context.offers.firstWhere(
      (offer) => offer.id != context.aiChoice.id,
    );
    return PlayerSponsorOfferChoice(offerId: alternative.id);
  }
}

class _CountingNonAiSponsorProvider extends PlayerSponsorDecisionProvider {
  int calls = 0;

  @override
  PlayerSponsorOfferChoice choose(PlayerSponsorDecisionContext context) {
    calls++;
    final alternative = context.offers.firstWhere(
      (offer) => offer.id != context.aiChoice.id,
    );
    return PlayerSponsorOfferChoice(offerId: alternative.id);
  }
}

class _NonAiCrisisProvider extends PlayerCrisisDecisionProvider {
  const _NonAiCrisisProvider();

  @override
  PlayerCrisisActionChoice choose(PlayerCrisisDecisionContext context) {
    final alternative = context.availableDecisions.firstWhere(
      (decision) => decision.action != context.aiDecision.action,
    );
    return PlayerCrisisActionChoice(action: alternative.action);
  }
}

class _CountingNonAiCrisisProvider extends PlayerCrisisDecisionProvider {
  int calls = 0;

  @override
  PlayerCrisisActionChoice choose(PlayerCrisisDecisionContext context) {
    calls++;
    final alternative = context.availableDecisions.firstWhere(
      (decision) => decision.action != context.aiDecision.action,
    );
    return PlayerCrisisActionChoice(action: alternative.action);
  }
}

class _RotatingCrisisProvider extends PlayerCrisisDecisionProvider {
  const _RotatingCrisisProvider();

  @override
  PlayerCrisisActionChoice choose(PlayerCrisisDecisionContext context) {
    final index =
        (context.seasonIndex + context.scenario.type.index) %
            context.availableDecisions.length;
    return PlayerCrisisActionChoice(
      action: context.availableDecisions[index].action,
    );
  }
}

void main() {
  const config = SimulationConfig(careerSeed: 20260903);
  const codec = PlayerPresidentTicketPricingRuntimeSaveCodec();
  const forcedAi = CrisisDecisionEngine(activationThreshold: 0);
  const noCrisis = CrisisDecisionEngine(activationThreshold: 101);

  late FictionalWorldSetup world;
  late String interactiveClubId;

  setUpAll(() {
    world = const FictionalWorldFactory().build();
    final discovery =
        const PlayerPresidentTenureGatedPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      ticketAiPolicy: _AlwaysBalancedPolicy(),
    ).simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: world.clubs.first.id,
      seasonCount: 1,
      hasFutureSeasonAfterReport: true,
    );
    final boundary = discovery.boundaries.single;
    final source = boundary.source.source.sponsor.report.sourceReport;
    final mediaByClub = {
      for (final item in source.baselineMediaReport.seasons.single.clubs)
        item.clubId: item,
    };
    final investmentByClub = {
      for (final item in boundary.source.decisions) item.clubId: item,
    };
    interactiveClubId = source.promiseReport.snapshots
        .firstWhere(
          (snapshot) =>
              mediaByClub[snapshot.promise.clubId]!.statement != null &&
              PlayerPresidentPromiseGenerator.allowedPromiseTypes(
                snapshot.context,
              ).any((type) => type != snapshot.promise.type) &&
              investmentByClub[snapshot.promise.clubId]!.invested,
        )
        .promise
        .clubId;
  });

  test('M70 without crisis provider preserves M69 exactly', () {
    const m69 =
        PlayerPresidentTenureGatedFacilitySponsorPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      sponsorProvider: _NonAiSponsorProvider(),
      facilityProvider: _HoldFacilityProvider(),
      promiseProvider: _AlternativePromiseProvider(),
      mediaProvider: _AlternativeMediaProvider(),
      transferStrategyProvider: _YouthTransferProvider(),
      ticketPricingProvider: _PremiumTicketProvider(),
      ticketAiPolicy: _AlwaysBalancedPolicy(),
    );
    const m70 =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      sponsorProvider: _NonAiSponsorProvider(),
      facilityProvider: _HoldFacilityProvider(),
      promiseProvider: _AlternativePromiseProvider(),
      mediaProvider: _AlternativeMediaProvider(),
      transferStrategyProvider: _YouthTransferProvider(),
      ticketPricingProvider: _PremiumTicketProvider(),
      ticketAiPolicy: _AlwaysBalancedPolicy(),
    );

    final baseline = m69.simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 2,
    );
    final composed = m70.simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 2,
    );

    expect(codec.encode(composed.checkpoint), codec.encode(baseline.checkpoint));
    expect(
      composed.boundaries.map((item) => item.signature).toList(),
      baseline.boundaries.map((item) => item.signature).toList(),
    );
  });

  test('M70 composes seven player providers in one real season boundary', () {
    final crisis = _CountingNonAiCrisisProvider();
    final sponsor = _CountingNonAiSponsorProvider();
    final facility = _CountingHoldFacilityProvider();
    final promise = _CountingAlternativePromiseProvider();
    final media = _CountingAlternativeMediaProvider();
    final transfer = _CountingYouthTransferProvider();
    final ticket = _CountingPremiumTicketProvider();
    final result =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      crisisProvider: crisis,
      sponsorProvider: sponsor,
      facilityProvider: facility,
      promiseProvider: promise,
      mediaProvider: media,
      transferStrategyProvider: transfer,
      ticketPricingProvider: ticket,
      aiCrisisEngine: forcedAi,
      ticketAiPolicy: const _AlwaysBalancedPolicy(),
    ).simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 1,
      hasFutureSeasonAfterReport: true,
    );

    expect(crisis.calls, 1);
    expect(sponsor.calls, 1);
    expect(facility.calls, 1);
    expect(promise.calls, 1);
    expect(media.calls, 1);
    expect(transfer.calls, greaterThan(0));
    expect(ticket.calls, 1);
    expect(
      result.boundaries.single.decisionFor(interactiveClubId).providerCalled,
      isTrue,
    );
  });

  test('M70 crisis override changes one club, keeps 47 AI rows exact, and persists real effects', () {
    const baselineEngine =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      aiCrisisEngine: forcedAi,
      ticketAiPolicy: _AlwaysBalancedPolicy(),
    );
    const playerEngine =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      crisisProvider: _NonAiCrisisProvider(),
      aiCrisisEngine: forcedAi,
      ticketAiPolicy: _AlwaysBalancedPolicy(),
    );

    final baseline = baselineEngine.simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 1,
    );
    final player = playerEngine.simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 1,
    );

    final baselineCrisis = baseline.boundaries.single.source.source.crisis;
    final playerCrisis = player.boundaries.single.source.source.crisis;
    final baselineByClub = {
      for (final item in baselineCrisis.clubs)
        item.clubId: item.resolution!.signature,
    };
    final playerByClub = {
      for (final item in playerCrisis.clubs)
        item.clubId: item.resolution!.signature,
    };
    expect(playerByClub[interactiveClubId], isNot(baselineByClub[interactiveClubId]));
    final aiIds = baselineByClub.keys
        .where((id) => id != interactiveClubId)
        .toList(growable: false);
    expect(aiIds, hasLength(47));
    for (final id in aiIds) {
      expect(playerByClub[id], baselineByClub[id], reason: id);
    }

    final snapshot = playerCrisis.clubs
        .firstWhere((item) => item.clubId == interactiveClubId);
    final resolution = snapshot.resolution!;
    final finance = playerCrisis
        .checkpoint.presidentRuntime.runtime.runtime.world.nextSeasonFinanceStates
        .firstWhere((item) => item.clubId == interactiveClubId);
    final president = playerCrisis.checkpoint.presidentRuntime.clubs
        .firstWhere((item) => item.clubId == interactiveClubId);
    expect(finance.signature, resolution.finance.signature);
    expect(president.fanReputation.signature, resolution.fan.signature);
    expect(president.mediaReputation.signature, resolution.media.signature);
    expect(finance.debt, snapshot.context.finance.debt);
  });

  test('M70 does not call player crisis provider when no crisis exists', () {
    const baselineEngine =
        PlayerPresidentTenureGatedFacilitySponsorPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      crisisIntegration: CrisisRuntimeIntegrationEngine(
        decisionEngine: noCrisis,
      ),
    );
    final provider = _CountingNonAiCrisisProvider();
    final player =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      crisisProvider: provider,
      aiCrisisEngine: noCrisis,
    ).simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 2,
    );
    final baseline = baselineEngine.simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 2,
    );

    expect(provider.calls, 0);
    expect(codec.encode(player.checkpoint), codec.encode(baseline.checkpoint));
    expect(player.signature, baseline.signature);
  });

  test('M70 lost and incumbent mismatch both block crisis control', () {
    final seedCheckpoint =
        const PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      aiCrisisEngine: forcedAi,
    )
            .simulateWithCheckpoint(
              clubs: world.clubs,
              leagues: world.leagues,
              config: config,
              controlledClubId: interactiveClubId,
              seasonCount: 1,
              hasFutureSeasonAfterReport: true,
            )
            .checkpoint;

    final lostProvider = _CountingNonAiCrisisProvider();
    final lost = PlayerPresidentTicketPricingRuntimeCheckpoint(
      runtime: seedCheckpoint.runtime,
      tenureControl: PlayerPresidentTenureControlState(
        controlledClubId: interactiveClubId,
        playerPresidentId: 'm70-former-president',
        status: PlayerPresidentTenureControlStatus.lost,
        lostAtCompletedSeason: seedCheckpoint.completedSeasons,
        successorPresidentId: 'm70-successor-president',
      ),
    );
    final lostResult =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      crisisProvider: lostProvider,
      aiCrisisEngine: forcedAi,
    ).resume(
      checkpoint: lost,
      seasonCount: 1,
      hasFutureSeasonAfterReport: true,
    );
    expect(lostProvider.calls, 0);
    expect(lostResult.checkpoint.tenureControl.lost, isTrue);

    final mismatchProvider = _CountingNonAiCrisisProvider();
    final mismatch = PlayerPresidentTicketPricingRuntimeCheckpoint(
      runtime: seedCheckpoint.runtime,
      tenureControl: PlayerPresidentTenureControlState(
        controlledClubId: interactiveClubId,
        playerPresidentId: 'm70-non-incumbent-president',
        status: PlayerPresidentTenureControlStatus.active,
      ),
    );
    final mismatchResult =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      crisisProvider: mismatchProvider,
      aiCrisisEngine: forcedAi,
    ).resume(
      checkpoint: mismatch,
      seasonCount: 1,
      hasFutureSeasonAfterReport: true,
    );
    expect(mismatchProvider.calls, 0);
    expect(mismatchResult.checkpoint.tenureControl.lost, isTrue);
  });

  test('M70 M65 codec keeps 2 plus 2 resume deterministic against four seasons', () {
    const engine =
        PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
      crisisProvider: _RotatingCrisisProvider(),
      sponsorProvider: _NonAiSponsorProvider(),
      facilityProvider: _HoldFacilityProvider(),
      promiseProvider: _AlternativePromiseProvider(),
      mediaProvider: _AlternativeMediaProvider(),
      transferStrategyProvider: _YouthTransferProvider(),
      ticketPricingProvider: _PremiumTicketProvider(),
      aiCrisisEngine: forcedAi,
    );
    final direct = engine.simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 4,
      electionInterval: 4,
    );
    final first = engine.simulateWithCheckpoint(
      clubs: world.clubs,
      leagues: world.leagues,
      config: config,
      controlledClubId: interactiveClubId,
      seasonCount: 2,
      electionInterval: 4,
      hasFutureSeasonAfterReport: true,
    );
    final encoded = codec.encode(first.checkpoint);
    final loaded = codec.decode(encoded);
    final second = engine.resume(
      checkpoint: loaded,
      seasonCount: 2,
    );

    expect(codec.encode(loaded), encoded);
    expect(codec.encode(second.checkpoint), codec.encode(direct.checkpoint));
    expect(
      [...first.boundaries, ...second.boundaries]
          .map((item) => item.signature)
          .toList(),
      direct.boundaries.map((item) => item.signature).toList(),
    );
  });
}
''',
          )

          write(
              'tool/run_m70_player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart',
              r'''import 'package:futbol_baskanlik_m0/futbol_baskanlik_m0.dart';
import 'package:futbol_baskanlik_m0/player_president_crisis_control.dart';
import 'package:futbol_baskanlik_m0/player_president_facility_control.dart';
import 'package:futbol_baskanlik_m0/player_president_media_statement_control.dart';
import 'package:futbol_baskanlik_m0/player_president_promise_control.dart';
import 'package:futbol_baskanlik_m0/player_president_sponsor_control.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_promise_media_transfer_ticket_pricing_runtime_composition.dart';
import 'package:futbol_baskanlik_m0/player_president_tenure_gated_ticket_pricing_runtime_integration.dart';
import 'package:futbol_baskanlik_m0/player_president_transfer_strategy_control.dart';

class _AlwaysBalancedPolicy extends PresidentMatchdayTicketPricingPolicy {
  const _AlwaysBalancedPolicy();

  @override
  MatchdayTicketPricingChoice choose({
    required PresidentManagementProfile profile,
    required int fanTrust,
    required StadiumAttendanceProfile base,
  }) =>
      const MatchdayTicketPricingChoice(MatchdayTicketPriceTier.balanced);
}

class _PremiumTicketProvider extends PlayerMatchdayTicketPricingDecisionProvider {
  const _PremiumTicketProvider();

  @override
  MatchdayTicketPricingChoice choose(PlayerPresidentTicketPricingDecisionContext context) =>
      const MatchdayTicketPricingChoice(MatchdayTicketPriceTier.premium);
}

class _CountingPremiumTicketProvider extends PlayerMatchdayTicketPricingDecisionProvider {
  int calls = 0;

  @override
  MatchdayTicketPricingChoice choose(PlayerPresidentTicketPricingDecisionContext context) {
    calls++;
    return const MatchdayTicketPricingChoice(MatchdayTicketPriceTier.premium);
  }
}

class _YouthTransferProvider extends PlayerTransferStrategyDecisionProvider {
  const _YouthTransferProvider();

  @override
  PlayerTransferStrategyChoice chooseTransferStrategy(PlayerTransferStrategyDecisionContext context) =>
      const PlayerTransferStrategyChoice(
        financialDiscipline: 60,
        transferAmbition: 60,
        riskAppetite: 60,
        youthOrientation: 90,
      );
}

class _CountingYouthTransferProvider extends PlayerTransferStrategyDecisionProvider {
  int calls = 0;

  @override
  PlayerTransferStrategyChoice chooseTransferStrategy(PlayerTransferStrategyDecisionContext context) {
    calls++;
    return const PlayerTransferStrategyChoice(
      financialDiscipline: 60,
      transferAmbition: 60,
      riskAppetite: 60,
      youthOrientation: 90,
    );
  }
}

class _AlternativePromiseProvider extends PlayerPromiseDecisionProvider {
  const _AlternativePromiseProvider();

  @override
  PresidentPromiseType choosePromise(PlayerPromiseDecisionContext context) =>
      context.allowedTypes.firstWhere(
        (type) => type != context.aiPromise.type,
        orElse: () => context.aiPromise.type,
      );
}

class _CountingAlternativePromiseProvider extends PlayerPromiseDecisionProvider {
  int calls = 0;

  @override
  PresidentPromiseType choosePromise(PlayerPromiseDecisionContext context) {
    calls++;
    return context.allowedTypes.firstWhere(
      (type) => type != context.aiPromise.type,
      orElse: () => context.aiPromise.type,
    );
  }
}

class _AlternativeMediaProvider extends PlayerMediaStatementDecisionProvider {
  const _AlternativeMediaProvider();

  @override
  MediaStance chooseStance(PlayerMediaStatementDecisionContext context) =>
      context.allowedStances.firstWhere((stance) => stance != context.aiStatement.stance);
}

class _CountingAlternativeMediaProvider extends PlayerMediaStatementDecisionProvider {
  int calls = 0;

  @override
  MediaStance chooseStance(PlayerMediaStatementDecisionContext context) {
    calls++;
    return context.allowedStances.firstWhere((stance) => stance != context.aiStatement.stance);
  }
}

class _HoldFacilityProvider extends PlayerFacilityInvestmentDecisionProvider {
  const _HoldFacilityProvider();

  @override
  PlayerFacilityInvestmentChoice choose(PlayerFacilityInvestmentContext context) =>
      PlayerFacilityInvestmentChoice.hold;
}

class _CountingHoldFacilityProvider extends PlayerFacilityInvestmentDecisionProvider {
  int calls = 0;

  @override
  PlayerFacilityInvestmentChoice choose(PlayerFacilityInvestmentContext context) {
    calls++;
    return PlayerFacilityInvestmentChoice.hold;
  }
}

class _NonAiSponsorProvider extends PlayerSponsorDecisionProvider {
  const _NonAiSponsorProvider();

  @override
  PlayerSponsorOfferChoice choose(PlayerSponsorDecisionContext context) {
    final alternative = context.offers.firstWhere((offer) => offer.id != context.aiChoice.id);
    return PlayerSponsorOfferChoice(offerId: alternative.id);
  }
}

class _CountingNonAiSponsorProvider extends PlayerSponsorDecisionProvider {
  int calls = 0;

  @override
  PlayerSponsorOfferChoice choose(PlayerSponsorDecisionContext context) {
    calls++;
    final alternative = context.offers.firstWhere((offer) => offer.id != context.aiChoice.id);
    return PlayerSponsorOfferChoice(offerId: alternative.id);
  }
}

class _NonAiCrisisProvider extends PlayerCrisisDecisionProvider {
  const _NonAiCrisisProvider();

  @override
  PlayerCrisisActionChoice choose(PlayerCrisisDecisionContext context) {
    final alternative = context.availableDecisions.firstWhere(
      (decision) => decision.action != context.aiDecision.action,
    );
    return PlayerCrisisActionChoice(action: alternative.action);
  }
}

class _CountingNonAiCrisisProvider extends PlayerCrisisDecisionProvider {
  int calls = 0;

  @override
  PlayerCrisisActionChoice choose(PlayerCrisisDecisionContext context) {
    calls++;
    final alternative = context.availableDecisions.firstWhere(
      (decision) => decision.action != context.aiDecision.action,
    );
    return PlayerCrisisActionChoice(action: alternative.action);
  }
}

class _RotatingCrisisProvider extends PlayerCrisisDecisionProvider {
  const _RotatingCrisisProvider();

  @override
  PlayerCrisisActionChoice choose(PlayerCrisisDecisionContext context) {
    final index =
        (context.seasonIndex + context.scenario.type.index) % context.availableDecisions.length;
    return PlayerCrisisActionChoice(action: context.availableDecisions[index].action);
  }
}

void main(List<String> args) {
  final seed = args.isEmpty ? 20260903 : int.parse(args.first);
  final config = SimulationConfig(careerSeed: seed);
  final world = const FictionalWorldFactory().build();
  const codec = PlayerPresidentTicketPricingRuntimeSaveCodec();
  const forcedAi = CrisisDecisionEngine(activationThreshold: 0);

  final discovery =
      const PlayerPresidentTenureGatedPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    ticketAiPolicy: _AlwaysBalancedPolicy(),
  ).simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: world.clubs.first.id,
    seasonCount: 1,
    hasFutureSeasonAfterReport: true,
  );
  final discoveryBoundary = discovery.boundaries.single;
  final source = discoveryBoundary.source.source.sponsor.report.sourceReport;
  final mediaByClub = {
    for (final item in source.baselineMediaReport.seasons.single.clubs) item.clubId: item,
  };
  final investmentByClub = {
    for (final item in discoveryBoundary.source.decisions) item.clubId: item,
  };
  final controlled = source.promiseReport.snapshots
      .firstWhere(
        (snapshot) =>
            mediaByClub[snapshot.promise.clubId]!.statement != null &&
            PlayerPresidentPromiseGenerator.allowedPromiseTypes(snapshot.context)
                .any((type) => type != snapshot.promise.type) &&
            investmentByClub[snapshot.promise.clubId]!.invested,
      )
      .promise
      .clubId;

  const m69 =
      PlayerPresidentTenureGatedFacilitySponsorPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    sponsorProvider: _NonAiSponsorProvider(),
    facilityProvider: _HoldFacilityProvider(),
    promiseProvider: _AlternativePromiseProvider(),
    mediaProvider: _AlternativeMediaProvider(),
    transferStrategyProvider: _YouthTransferProvider(),
    ticketPricingProvider: _PremiumTicketProvider(),
    ticketAiPolicy: _AlwaysBalancedPolicy(),
  );
  const parityEngine =
      PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    sponsorProvider: _NonAiSponsorProvider(),
    facilityProvider: _HoldFacilityProvider(),
    promiseProvider: _AlternativePromiseProvider(),
    mediaProvider: _AlternativeMediaProvider(),
    transferStrategyProvider: _YouthTransferProvider(),
    ticketPricingProvider: _PremiumTicketProvider(),
    ticketAiPolicy: _AlwaysBalancedPolicy(),
  );
  final baseline = m69.simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: controlled,
    seasonCount: 2,
  );
  final parity = parityEngine.simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: controlled,
    seasonCount: 2,
  );
  final m69ParityWithoutCrisis =
      codec.encode(parity.checkpoint) == codec.encode(baseline.checkpoint) &&
          parity.signature == baseline.signature;

  final crisis = _CountingNonAiCrisisProvider();
  final sponsor = _CountingNonAiSponsorProvider();
  final facility = _CountingHoldFacilityProvider();
  final promise = _CountingAlternativePromiseProvider();
  final media = _CountingAlternativeMediaProvider();
  final transfer = _CountingYouthTransferProvider();
  final ticket = _CountingPremiumTicketProvider();
  final composed =
      PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    crisisProvider: crisis,
    sponsorProvider: sponsor,
    facilityProvider: facility,
    promiseProvider: promise,
    mediaProvider: media,
    transferStrategyProvider: transfer,
    ticketPricingProvider: ticket,
    aiCrisisEngine: forcedAi,
    ticketAiPolicy: const _AlwaysBalancedPolicy(),
  ).simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: controlled,
    seasonCount: 1,
    hasFutureSeasonAfterReport: true,
  );
  final sevenProviders = crisis.calls == 1 &&
      sponsor.calls == 1 &&
      facility.calls == 1 &&
      promise.calls == 1 &&
      media.calls == 1 &&
      transfer.calls > 0 &&
      ticket.calls == 1;

  final aiBaseline =
      const PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    aiCrisisEngine: forcedAi,
  ).simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: controlled,
    seasonCount: 1,
  );
  final crisisOnly =
      const PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    crisisProvider: _NonAiCrisisProvider(),
    aiCrisisEngine: forcedAi,
  ).simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: controlled,
    seasonCount: 1,
  );
  final baselineCrisis = aiBaseline.boundaries.single.source.source.crisis;
  final playerCrisis = crisisOnly.boundaries.single.source.source.crisis;
  final baselineByClub = {
    for (final item in baselineCrisis.clubs) item.clubId: item.resolution!.signature,
  };
  final playerByClub = {
    for (final item in playerCrisis.clubs) item.clubId: item.resolution!.signature,
  };
  final aiIds = baselineByClub.keys.where((id) => id != controlled);
  final aiParity47 = aiIds.length == 47 &&
      aiIds.every((id) => playerByClub[id] == baselineByClub[id]);
  final crisisChanged = playerByClub[controlled] != baselineByClub[controlled];
  final snapshot = playerCrisis.clubs.firstWhere((item) => item.clubId == controlled);
  final resolution = snapshot.resolution!;
  final finance = playerCrisis
      .checkpoint.presidentRuntime.runtime.runtime.world.nextSeasonFinanceStates
      .firstWhere((item) => item.clubId == controlled);
  final president = playerCrisis.checkpoint.presidentRuntime.clubs
      .firstWhere((item) => item.clubId == controlled);
  final realCrisisState = finance.signature == resolution.finance.signature &&
      president.fanReputation.signature == resolution.fan.signature &&
      president.mediaReputation.signature == resolution.media.signature;

  final seedCheckpoint =
      const PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    aiCrisisEngine: forcedAi,
  )
          .simulateWithCheckpoint(
            clubs: world.clubs,
            leagues: world.leagues,
            config: config,
            controlledClubId: controlled,
            seasonCount: 1,
            hasFutureSeasonAfterReport: true,
          )
          .checkpoint;
  final lostCheckpoint = PlayerPresidentTicketPricingRuntimeCheckpoint(
    runtime: seedCheckpoint.runtime,
    tenureControl: PlayerPresidentTenureControlState(
      controlledClubId: controlled,
      playerPresidentId: 'm70-former-president',
      status: PlayerPresidentTenureControlStatus.lost,
      lostAtCompletedSeason: seedCheckpoint.completedSeasons,
      successorPresidentId: 'm70-successor-president',
    ),
  );
  final blockedCrisis = _CountingNonAiCrisisProvider();
  final blocked =
      PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    crisisProvider: blockedCrisis,
    aiCrisisEngine: forcedAi,
  ).resume(
    checkpoint: lostCheckpoint,
    seasonCount: 1,
    hasFutureSeasonAfterReport: true,
  );
  final lostBlocksCrisis = blockedCrisis.calls == 0 && blocked.checkpoint.tenureControl.lost;

  const deterministicEngine =
      PlayerPresidentTenureGatedFacilitySponsorCrisisPromiseMediaTransferTicketPricingRuntimeCareerEngine(
    crisisProvider: _RotatingCrisisProvider(),
    sponsorProvider: _NonAiSponsorProvider(),
    facilityProvider: _HoldFacilityProvider(),
    promiseProvider: _AlternativePromiseProvider(),
    mediaProvider: _AlternativeMediaProvider(),
    transferStrategyProvider: _YouthTransferProvider(),
    ticketPricingProvider: _PremiumTicketProvider(),
    aiCrisisEngine: forcedAi,
  );
  final direct = deterministicEngine.simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: controlled,
    seasonCount: 4,
    electionInterval: 4,
  );
  final first = deterministicEngine.simulateWithCheckpoint(
    clubs: world.clubs,
    leagues: world.leagues,
    config: config,
    controlledClubId: controlled,
    seasonCount: 2,
    electionInterval: 4,
    hasFutureSeasonAfterReport: true,
  );
  final loaded = codec.decode(codec.encode(first.checkpoint));
  final resumed = deterministicEngine.resume(checkpoint: loaded, seasonCount: 2);
  final saveResume =
      codec.encode(resumed.checkpoint) == codec.encode(direct.checkpoint) &&
          [...first.boundaries, ...resumed.boundaries]
                  .map((item) => item.signature)
                  .join('||') ==
              direct.boundaries.map((item) => item.signature).join('||');

  if (!m69ParityWithoutCrisis ||
      !sevenProviders ||
      !crisisChanged ||
      !aiParity47 ||
      !realCrisisState ||
      !lostBlocksCrisis ||
      !saveResume) {
    throw StateError(
      'M70 canonical failure: m69Parity=$m69ParityWithoutCrisis '
      'sevenProviders=$sevenProviders crisisChanged=$crisisChanged '
      'aiParity47=$aiParity47 realCrisisState=$realCrisisState '
      'lostBlocksCrisis=$lostBlocksCrisis saveResume=$saveResume',
    );
  }

  print(
    'M70_PLAYER_PRESIDENT_TENURE_GATED_FACILITY_SPONSOR_CRISIS_PROMISE_MEDIA_TRANSFER_TICKET_PRICING_RUNTIME_COMPOSITION_PASS '
    'controlled=$controlled m69ParityWithoutCrisis=$m69ParityWithoutCrisis '
    'sevenProviders=$sevenProviders crisisChanged=$crisisChanged '
    'aiParity=47 realCrisisState=$realCrisisState '
    'lostBlocksCrisis=$lostBlocksCrisis singleCheckpoint=true '
    'saveResume=$saveResume worldClubs=${world.clubs.length} seed=$seed',
  );
}
''',
          )

          write(
              'M70_PLAYER_PRESIDENT_TENURE_GATED_FACILITY_SPONSOR_CRISIS_PROMISE_MEDIA_TRANSFER_TICKET_PRICING_RUNTIME_COMPOSITION_I.md',
              r'''# M70 — Player President Tenure-Gated Facility + Sponsor + Crisis + Promise/Media + Transfer/Ticket Pricing Runtime Composition I

## Amaç

M51 player-president crisis response kontrolünü M69'un gerçek sezon runtime'ına yeni checkpoint veya save codec oluşturmadan eklemek.

## Tasarım

- M65 `PlayerPresidentTicketPricingRuntimeCheckpoint` ve `PlayerPresidentTicketPricingRuntimeSaveCodec` authoritative kalır.
- M65→M68→M69 zincirine default-neutral `CrisisRuntimeIntegrationEngine` injection seam'i geçirilir.
- Crisis provider yokken M69 exact parity korunur.
- Crisis provider yalnız controlled club için, gerçek crisis tespit edildiğinde ve persisted player-president incumbent kimliği hâlâ geçerliyken çağrılır.
- Persisted loss başlangıçtan bloklar; incumbent mismatch aynı runtime oturumunda sticky biçimde AI fallback'e geçer.
- Diğer 47 kulüp exact canonical M43 AI crisis yolunda kalır.
- Seçilen crisis action M44/M47 üzerinden gerçek finance/fan/media continuation state'ine yazılır.
- Crisis provider runtime-only'dir; serialize edilmez.
- Facility + sponsor + crisis + promise + media + transfer strategy + ticket pricing aynı M65 persisted tenure/checkpoint akışında compose edilir.
- Manager player-control entegrasyonu M70 kapsamı dışındadır.

## Acceptance

1. Crisis provider yokken M69 checkpoint ve boundary exact parity.
2. Yedi player provider aynı gerçek sezon boundary'sinde çalışır.
3. Crisis override yalnız controlled club'u değiştirir; diğer 47 club exact AI crisis parity'de kalır ve seçilen action gerçek continuation state'ine yazılır.
4. Crisis yoksa player crisis provider çağrılmaz ve AI runtime parity korunur.
5. Persisted loss ve incumbent mismatch crisis player-control'u bloklar; mismatch persisted tenure loss'a dönüşür.
6. M65 codec ile 2+2 save/resume, uninterrupted 4-season deterministic run ile aynıdır.

## CI hedefi

- analyzer clean
- 315/315 non-canonical tests PASS
- M0–M70 canonical PASS
- M70 marker PASS
- artifact 0
''',
          )

          replace_once(
              '.github/workflows/m0-tests.yml',
              "      - name: Run M69 player president facility sponsor promise media transfer ticket runtime composition\n        run: dart run tool/run_m69_player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart 20260903\n",
              "      - name: Run M69 player president facility sponsor promise media transfer ticket runtime composition\n        run: dart run tool/run_m69_player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart 20260903\n\n      - name: Run M70 player president facility sponsor crisis promise media transfer ticket runtime composition\n        run: dart run tool/run_m70_player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart 20260903\n",
          )

          replace_once(
              'GENEL_PROJE_OZETI.md',
              "**M0–M69 CLOSED / MERGED / PASS ve `main` üzerindedir.**\n\n**Aktif milestone yok.**",
              "**M0–M69 CLOSED / MERGED / PASS ve `main` üzerindedir.**\n\n**Aktif milestone: M70 — Player President Tenure-Gated Facility + Sponsor + Crisis + Promise/Media + Transfer/Ticket Pricing Runtime Composition I — IMPLEMENTATION / CI PENDING.**\n\nM70 canlı `main` taramasında M59'un M69/M65 authoritative checkpoint dışında kalan iki player-control parçasından crisis yolunun, manager yoluna göre daha dar ve mevcut M47 `CrisisRuntimeIntegrationEngine` seam'i üzerinden güvenli biçimde compose edilebilir olduğu doğrulanarak seçildi. M70 yeni checkpoint/save codec yaratmaz; M65 authoritative kalır. Crisis provider yalnız controlled club, gerçek crisis ve persisted incumbent ownership eşleşmesinde çalışır; diğer 47 kulüp canonical AI parity'de kalır. Manager player-control entegrasyonu kapsam dışıdır."
          )
          PY

      - name: Format changed Dart files
        run: >-
          dart format
          lib/src/facility/player_president_tenure_gated_ticket_pricing_runtime_integration.dart
          lib/src/facility/player_president_tenure_gated_facility_promise_media_transfer_ticket_pricing_runtime_composition.dart
          lib/src/sponsor/player_president_tenure_gated_facility_sponsor_promise_media_transfer_ticket_pricing_runtime_composition.dart
          lib/src/crisis/player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart
          lib/player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart
          test/m70_player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition_test.dart
          tool/run_m70_player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart

      - name: Analyze
        run: dart analyze

      - name: Run M70 acceptance tests
        run: dart test test/m70_player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition_test.dart

      - name: Run M70 canonical marker
        run: dart run tool/run_m70_player_president_tenure_gated_facility_sponsor_crisis_promise_media_transfer_ticket_pricing_runtime_composition.dart 20260903

      - name: Commit verified M70 implementation
        shell: bash
        run: |
          rm .github/workflows/m70-bootstrap.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(m70): compose crisis control into unified player-president runtime"
          git push origin HEAD:feat/m70-crisis-unified-player-president-runtime
